import classes from './Logo.module.css';

const Logo = () => {
  return (
    <span className={classes.container}>
      <span className={classes.mak}>3 Mak</span>
      &nbsp;Granite
    </span>
  );
};

export default Logo;
